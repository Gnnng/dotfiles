language: generic

cache:
    directories:
        - $HOME/.vim/plugged

os:
    - osx
    - linux

script:
    - cd $HOME && git clone https://github.com/Gnnng/dotfiles
    - cd $HOME/dotfiles && ./install.sh
